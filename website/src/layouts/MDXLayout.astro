---
import PostOuter from '@/layouts/PostOuter.astro';
const { frontmatter, headings } = Astro.props;
const { title } = frontmatter;
---

<PostOuter {title} {headings} id={title}>
  <slot />
</PostOuter>
